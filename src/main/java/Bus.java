
public class Bus extends Car {
    public Bus(String name, int size, int govNumber) {
        super(name, size, govNumber);
    }
    public String getInfo(){
        String BusInfo = super.getInfo();
        return BusInfo;
    }
}
